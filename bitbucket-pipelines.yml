image: alpine/git:latest

pipelines:
  default:
    - step:
        name: Sync Current Branch to GitHub
        script:
          # Pushes ONLY the branch that triggered this pipeline
          - git push git@github.com:awaisahmad-innovinci/blackbox.git HEAD:refs/heads/$BITBUCKET_BRANCH