-- Initial schema placeholder for Blackbox.
-- Add tables, policies, and functions in subsequent migrations via:
--   supabase migration new <name>

-- Ensure the public schema exists (default on Postgres / Supabase).
create schema if not exists public;

comment on schema public is 'Blackbox application schema';
